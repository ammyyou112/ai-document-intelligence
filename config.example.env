# DeepSeek OCR API Configuration
# Get your API key from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_api_key_here

# Local model/runtime configuration
# Choose runtime backend: "transformers" or "vllm"
MODEL_BACKEND=transformers
# Hugging Face model id
MODEL_NAME=deepseek-ai/DeepSeek-OCR
# GPU visibility (set to -1 for CPU-only)
CUDA_VISIBLE_DEVICES=0
# Output directory for model artifacts/results
OUTPUT_DIR=outputs
# Image processing defaults (align with DeepSeek-OCR docs)
BASE_SIZE=1024
IMAGE_SIZE=640
# Use FlashAttention if available (true/false)
USE_FLASH_ATTENTION=true

# Optional: Custom API URL (default: https://api.deepseek.com/v1/chat/completions)
# DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions


